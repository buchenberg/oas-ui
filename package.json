{
  "name": "oas-ui",
  "version": "0.0.2",
  "description": "",
  "main": "server.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "postinstall": "cd lib/ui && npm install && npm run build",
    "dev-client": "cd lib/ui && npm start",
    "dev-server": "nodemon server.js",
    "dev": "concurrently 'npm run dev-server' 'npm run dev-client'"
  },
  "author": "G. Buchenberger",
  "license": "ISC",
  "dependencies": {
    "glue": "^5.0.0",
    "inert": "^5.0.1",
    "vision": "^5.2.0"
  },
  "devDependencies": {
    "concurrently": "^3.5.0",
    "nodemon": "^1.12.1"
  }
}
